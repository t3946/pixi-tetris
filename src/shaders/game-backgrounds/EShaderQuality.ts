/** Качество фоновых шейдеров. Переключение — в настройках. */
export enum EShaderQuality {
    High = 'high',
    Low = 'low',
}

/** Основной режим по умолчанию. */
export const DEFAULT_SHADER_QUALITY = EShaderQuality.High
